# ══════════════════════════════════════════════════════════════════════════════
# DECAP CMS - Configuration pour MGC Coaching
# ══════════════════════════════════════════════════════════════════════════════

# ⚠️  DÉCOMMENTER POUR TEST LOCAL
local_backend: true

backend:
  name: git-gateway
  repo: MGCCoaching/mgc-website
  branch: mod
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  auth_endpoint: /sites/5cdd74d8-4be1-4c81-961d-5930b10bef6f/pkce
  auth_token_endpoint: /sites/5cdd74d8-4be1-4c81-961d-5930b10bef6f/token
  gateway_url: https://gateway.decapbridge.com

  commit_messages:
    create: Create {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

auth:
  email_claim: email
  first_name_claim: first_name
  last_name_claim: last_name
  avatar_url_claim: avatar_url

logo_url: /img/logo.webp
site_url: https://mariegaetanecomte.fr

media_folder: "static/img/uploads"
public_folder: "/img/uploads"

locale: "fr"

slug:
  encoding: "ascii"
  clean_accents: true
  sanitize_replacement: "-"

# ══════════════════════════════════════════════════════════════════════════════
# COLLECTIONS
# ══════════════════════════════════════════════════════════════════════════════

collections:

  # ────────────────────────────────────────────────────────────────────────────
  # PODCASTS (Épisodes)
  # ────────────────────────────────────────────────────────────────────────────
  - name: "podcasts"
    label: "🎙️ Épisodes de Podcast"
    label_singular: "Épisode"
    folder: "content/podcasts"
    create: true
    slug: "{{slug}}"
    path: "{{slug}}/index"
    media_folder: ""
    public_folder: ""
    filter:
      field: is_episode
      value: true
    summary: "S{{season}}E{{episode_number}} - {{title}}"
    sortable_fields: ["date", "episode_number", "title"]
    view_groups:
      - label: "Saison"
        field: season
    fields:
      # ══════════════════════════════════════════════════════════════════
      # CHAMPS CACHÉS (système)
      # ══════════════════════════════════════════════════════════════════
      - label: "Outputs"
        name: "outputs"
        widget: "hidden"
        default: ["html", "data", "chapters"]
      
      - label: "Catégories"
        name: "categories"
        widget: "hidden"
        default: ["Podcast"]
      
      - label: "Tags"
        name: "tags"
        widget: "hidden"
        default: ["podcast"]
      
      - label: "Format audio"
        name: "audio_format"
        widget: "hidden"
        default: "audio/mpeg"

      # ══════════════════════════════════════════════════════════════════
      # INFORMATIONS PRINCIPALES
      # ══════════════════════════════════════════════════════════════════
      - label: "📌 Titre de l'épisode"
        name: "title"
        widget: "string"
        required: true

      - label: "📝 Sous-titre"
        name: "subtitle"
        widget: "string"
        required: false
        hint: "Ex: Interview avec Jean Dupont"

      - label: "📅 Date de publication"
        name: "date"
        widget: "datetime"
        format: "YYYY-MM-DD"
        date_format: "DD/MM/YYYY"
        time_format: false

      - label: "✏️ Brouillon"
        name: "draft"
        widget: "boolean"
        default: true
        hint: "⚠️ Décochez quand l'épisode est prêt"

      # ══════════════════════════════════════════════════════════════════
      # NUMÉROTATION
      # ══════════════════════════════════════════════════════════════════
      - label: "🔢 Saison"
        name: "season"
        widget: "number"
        default: 1
        value_type: "int"
        min: 1

      - label: "🔢 Numéro d'épisode"
        name: "episode_number"
        widget: "number"
        required: true
        value_type: "int"
        min: 1

      - label: "📁 Type d'épisode"
        name: "episode_type"
        widget: "select"
        options:
          - { label: "Épisode normal", value: "full" }
          - { label: "Bande-annonce", value: "trailer" }
          - { label: "Bonus", value: "bonus" }
        default: "full"

      # ══════════════════════════════════════════════════════════════════
      # FICHIER AUDIO
      # ══════════════════════════════════════════════════════════════════
      - label: "🎵 Hébergement audio"
        name: "audio_url_prefix"
        widget: "select"
        required: false
        options:
          - { label: "CDN (recommandé)", value: "https://media.mariegaetanecomte.fr/podcasts" }
          - { label: "Upload local", value: "" }
        hint: "CDN = plus rapide, Upload = fichier dans le site"

      - label: "📄 Nom du fichier audio"
        name: "file_name"
        widget: "string"
        required: false
        hint: "Ex: ep_01.mp3 (pour CDN uniquement)"

      - label: "🎵 Fichier audio (upload)"
        name: "audio_file"
        widget: "file"
        required: false
        media_folder: ""
        hint: "Uniquement si 'Upload local' sélectionné"

      - label: "⏱️ Durée"
        name: "duration"
        widget: "string"
        required: true
        default: "00:00"
        hint: "Format MM:SS ou HH:MM:SS"

      # ══════════════════════════════════════════════════════════════════
      # IMAGE & DESCRIPTION
      # ══════════════════════════════════════════════════════════════════
      - label: "🖼️ Image de couverture"
        name: "cover"
        widget: "image"
        required: false
        hint: "1400x1400px recommandé. Sinon : première image du dossier ou fallback."

      - label: "📄 Description"
        name: "description"
        widget: "text"
        required: true
        hint: "2-3 phrases pour Spotify, Apple Podcasts, etc."

      # ══════════════════════════════════════════════════════════════════
      # INVITÉ(E)S
      # ══════════════════════════════════════════════════════════════════
      - label: "👤 Invité(e)s"
        name: "guests"
        widget: "list"
        required: false
        collapsed: true
        allow_add: true
        hint: "Nom (Fonction)"

      # ══════════════════════════════════════════════════════════════════
      # CHAPITRES
      # ══════════════════════════════════════════════════════════════════
      - label: "📑 Chapitres"
        name: "chapters"
        widget: "list"
        required: false
        collapsed: true
        hint: "Navigation dans l'épisode"
        summary: "{{fields.time}} - {{fields.title}}"
        fields:
          - label: "Temps"
            name: "time"
            widget: "string"
            hint: "Ex: 00:00, 05:30, 12:45"
          - label: "Titre"
            name: "title"
            widget: "string"

      # ══════════════════════════════════════════════════════════════════
      # NOTES DE L'ÉPISODE
      # ══════════════════════════════════════════════════════════════════
      - label: "📝 Notes de l'épisode"
        name: "body"
        widget: "markdown"
        required: false
        hint: "Résumé, points clés, liens..."

      # ══════════════════════════════════════════════════════════════════
      # OPTIONS
      # ══════════════════════════════════════════════════════════════════
      - label: "🔞 Contenu explicite"
        name: "explicit"
        widget: "boolean"
        default: false
        required: false

      - label: "🚫 Masquer du RSS"
        name: "block"
        widget: "boolean"
        default: false
        required: false
        hint: "Exclure des plateformes (Spotify, Apple...)"

      # ══════════════════════════════════════════════════════════════════
      # LIENS PLATEFORMES
      # ══════════════════════════════════════════════════════════════════
      - label: "🔗 Liens plateformes"
        name: "platforms"
        widget: "object"
        collapsed: true
        fields:
          - { label: "Spotify", name: "spotify", widget: "string", required: false }
          - { label: "Apple Podcasts", name: "apple_podcasts", widget: "string", required: false }
          - { label: "Deezer", name: "deezer", widget: "string", required: false }
          - { label: "YouTube", name: "youtube", widget: "string", required: false }

  # ────────────────────────────────────────────────────────────────────────────
  # SECTIONS PAGE D'ACCUEIL
  # ────────────────────────────────────────────────────────────────────────────
  - name: "home_sections"
    label: "🏠 Sections Page d'accueil"
    label_singular: "Section"
    folder: "content/home-sections"
    create: false
    slug: "{{slug}}"
    sortable_fields: ["weight", "title"]
    fields:
      - { label: "Titre", name: "title", widget: "string" }
      - { label: "Type de section", name: "section", widget: "hidden" }
      - label: "Services"
        name: "features"
        widget: "list"
        required: false
        summary: "{{fields.title}}"
        fields:
          - { label: "Titre", name: "title", widget: "string" }
          - { label: "Icône", name: "icon", widget: "hidden" }
          - { label: "Lien", name: "button_url", widget: "hidden" }
          - { label: "Description", name: "description", widget: "text" }
      - { label: "Ordre", name: "weight", widget: "hidden" }
      - { label: "Texte bouton", name: "button_text", widget: "hidden", required: false }
      - { label: "Lien bouton", name: "button_url", widget: "hidden", required: false }
      - { label: "Image", name: "image", widget: "image", required: false }
      - { label: "Contenu", name: "body", widget: "markdown" }

  # ────────────────────────────────────────────────────────────────────────────
  # PRESTATIONS
  # ────────────────────────────────────────────────────────────────────────────
  - name: "features"
    label: "✨ Pages Services"
    folder: "content/features"
    create: false
    slug: "{{slug}}"
    fields:
      - { label: "Titre", name: "title", widget: "string" }
      - { label: "Sous-titre", name: "subtitle", widget: "string" }
      - { label: "Image", name: "image", widget: "image" }
      - { label: "Contenu", name: "body", widget: "markdown" }
      - { label: "Icône", name: "icon", widget: "hidden" }
      - { label: "Catégorie", name: "category", widget: "hidden" }
      - { label: "Poids", name: "weight", widget: "hidden" }

  # ────────────────────────────────────────────────────────────────────────────
  # PAGES
  # ────────────────────────────────────────────────────────────────────────────
  - name: "pages"
    label: "📄 Autres Pages"
    files:
      - label: "Page Podcasts"
        name: "podcasts"
        file: "content/podcasts/_index.md"
        fields:
          - { label: "Titre", name: "title", widget: "string" }
          - { label: "Description", name: "description", widget: "text", required: false }
          - { label: "Contenu", name: "body", widget: "markdown" }
      
      - label: "Contact"
        name: "contact"
        file: "content/contact.md"
        fields:
          - { label: "Titre", name: "title", widget: "string" }
          - { label: "Layout", name: "layout", widget: "hidden", default: "contact" }
          - { label: "Contenu", name: "body", widget: "markdown", required: false }
      
      - label: "Mentions légales"
        name: "legal"
        file: "content/legal.md"
        fields:
          - { label: "Titre", name: "title", widget: "string" }
          - { label: "Layout", name: "layout", widget: "hidden", default: "legal" }
          - { label: "Mise à jour", name: "lastUpdate", widget: "date", required: false }
          - { label: "Contenu", name: "body", widget: "markdown" }

  # ────────────────────────────────────────────────────────────────────────────
  # PARAMÈTRES
  # ────────────────────────────────────────────────────────────────────────────
  - name: "settings"
    label: "⚙️ Paramètres"
    files:
      - label: "🌐 Informations générales"
        name: "params"
        file: "config/_default/params.fr.toml"
        fields:
          - { label: "Description", name: "description", widget: "text" }
          - { label: "Auteur", name: "author", widget: "string" }
          - { label: "Sous-titre auteur", name: "author_subtitle", widget: "string" }
          
          - label: "Contact"
            name: "contact"
            widget: "object"
            fields:
              - { label: "Email", name: "email", widget: "string" }
              - { label: "Téléphone", name: "phone", widget: "string" }
              - { label: "Adresse", name: "address", widget: "string" }
          
          - label: "Réseaux sociaux"
            name: "social"
            widget: "object"
            fields:
              - { label: "LinkedIn", name: "linkedin", widget: "string", required: false }
              - { label: "Instagram", name: "instagram", widget: "string", required: false }

          - label: "🎙️ Podcast"
            name: "podcast"
            widget: "object"
            collapsed: true
            fields:
              - { label: "Titre", name: "title", widget: "string" }
              - { label: "Description", name: "description", widget: "text" }
              - { label: "Auteur", name: "author", widget: "string" }
              - { label: "Email", name: "email", widget: "string" }
              - { label: "Hôte", name: "host", widget: "string" }
              - { label: "Avatar hôte", name: "hostAvatar", widget: "image", required: false }
              - label: "Langue"
                name: "language"
                widget: "select"
                options: [{ label: "Français", value: "fr" }, { label: "English", value: "en" }]
                default: "fr"
              - { label: "Image couverture", name: "image", widget: "image", hint: "1400x1400 min" }
              - label: "Catégorie"
                name: "category"
                widget: "select"
                options: ["Arts", "Business", "Comedy", "Education", "Health & Fitness", "Leisure", "Music", "News", "Science", "Society & Culture", "Sports", "Technology", "True Crime"]
                default: "Business"
              - { label: "Sous-catégorie", name: "subcategory", widget: "string", required: false }
              - { label: "Explicite", name: "explicit", widget: "boolean", default: false }
              - { label: "Nom propriétaire", name: "owner_name", widget: "string" }
              - { label: "Email propriétaire", name: "owner_email", widget: "string" }
              - { label: "Lien soutien", name: "funding_url", widget: "string", required: false }